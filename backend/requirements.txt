# ============================================================
# ACE Voice Controller — Backend Core Dependencies
# Install with:
#   pip install --trusted-host pypi.org --trusted-host pypi.python.org --trusted-host files.pythonhosted.org -r requirements.txt
# ============================================================

# --- Web Framework ---
fastapi
uvicorn[standard]
python-multipart
websockets

# --- Supabase ---
supabase
httpx

# --- Auth & Security ---
python-jose[cryptography]
passlib[bcrypt]
cryptography

# --- Config ---
pydantic
pydantic[email]
pydantic-settings
python-dotenv

# --- Command Parsing ---
rapidfuzz

# --- LLM Providers (install only the ones you use) ---
groq
openai
google-generativeai
anthropic
ollama  # (Phase 2 — local models)

# --- Utilities ---
aiofiles
loguru
rich
pyautogui
fastembed
browser-use
symspellpy
# ============================================================
# Voice Stack — install separately after core is working:
#   pip install --trusted-host pypi.org ... -r requirements-voice.txt
# ============================================================
faster-whisper
openwakeword
piper-tts
pyaudio
numpy
webrtcvad
pygame
gTTS
# pyrnnoise

# ============================================================
# Automation Stack — install separately:
#   pip install --trusted-host pypi.org ... -r requirements-automation.txt
# ============================================================
pywinauto; sys_platform == 'win32'
pynput
psutil
playwright
playwright-stealth
watchfiles
pystray; sys_platform == 'win32'
Pillow
mss
pytesseract
schedule
winsdk; sys_platform == 'win32'
PyQt6; sys_platform == 'win32'
qasync; sys_platform == 'win32'
pyperclip
send2trash
beautifulsoup4
lxml
pycaw; sys_platform == 'win32'
plyer