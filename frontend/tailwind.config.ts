/**
 * tailwind.config.ts is NOT used by Tailwind CSS v4.
 * All theme configuration is now done inside globals.css using @theme {}.
 *
 * This file is kept only as a reference. The active config is:
 *   src/app/globals.css  →  @theme { ... }
 */
